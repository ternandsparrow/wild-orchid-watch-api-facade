{
  "name": "wow-api-facade",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "IS_DEV_MODE=${IS_DEV_MODE:-true} nodemon --ext js --watch . --watch package.json --exec node index.js",
    "start:debug": "IS_DEV_MODE=${IS_DEV_MODE:-true} node --inspect-brk index.js",
    "test": "jest"
  },
  "dependencies": {
    "@sentry/node": "^5.26.0",
    "@sentry/tracing": "^5.26.0",
    "axios": "^0.20.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^26.5.3",
    "nodemon": "^2.0.4"
  },
  "engines": {
    "npm": ">=6.14.6",
    "node": ">=12.18.3"
  }
}
